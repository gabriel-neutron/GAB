Write each reply and each document in ASD-STE100 Simplified Technical English.
Development happens on Windows only.
Read `docs/README.md` first. It routes you to the document that governs the task.
`docs/decisions.md` is locked. An entry changes only when a new decision names it and replaces it.
An open question is a tracker ticket. Stop, ask the operator, and write the code after the answer.
The operator owns `docs/`. A code task leaves every document as it found it, and puts each document change it needs under ASK.
