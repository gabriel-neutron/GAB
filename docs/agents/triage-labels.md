# Triage labels

This repo uses the five canonical label strings unchanged. When a skill names a triage
role, use the same string as the label.

`needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human` · `wontfix`
